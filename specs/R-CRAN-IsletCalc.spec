%global __brp_check_rpaths %{nil}
%global __requires_exclude ^libmpi
%global packname  IsletCalc
%global packver   0.0.1
%global rlibdir   /usr/local/lib/R/library

Name:             R-CRAN-%{packname}
Version:          0.0.1
Release:          1%{?dist}%{?buildtag}
Summary:          Calculators for Insulin and Glucagon Release Indices

License:          MIT + file LICENSE
URL:              https://cran.r-project.org/package=%{packname}
Source0:          %{url}&version=%{packver}#/%{packname}_%{packver}.tar.gz


BuildRequires:    R-devel >= 3.5.0
Requires:         R-core >= 3.5.0
BuildArch:        noarch

%description
Facilitates the calculation of validated pancreatic islet hormone-release
indices from fasting and oral glucose tolerance test (OGTT) measurements.
Provides beta-cell insulin release indices (including HOMA-beta, corrected
insulin response, Stumvoll first-phase index, BIGTT-AIR, and disposition
indices) as described in Madsen (2024) <doi:10.1038/s42255-024-01140-6>,
alongside alpha-cell glucagon release and glucagon resistance indices
derived from the glucagon-suppression and liver-alpha-cell-axis
literature. Enables reproducible assessment of beta-cell and alpha-cell
function for metabolic and endocrine research.

%prep
%setup -q -c -n %{packname}

# fix end of executable files
find -type f -executable -exec grep -Iq . {} \; -exec sed -i -e '$a\' {} \;
# prevent binary stripping
[ -d %{packname}/src ] && find %{packname}/src -type f -exec \
  sed -i 's@/usr/bin/strip@/usr/bin/true@g' {} \; || true
[ -d %{packname}/src ] && find %{packname}/src/Make* -type f -exec \
  sed -i 's@-g0@@g' {} \; || true
# don't allow local prefix in executable scripts
find -type f -executable -exec sed -Ei 's@#!( )*/usr/local/bin@#!/usr/bin@g' {} \;

%build

%install

mkdir -p %{buildroot}%{rlibdir}
%{_bindir}/R CMD INSTALL -l %{buildroot}%{rlibdir} %{packname}
test -d %{packname}/src && (cd %{packname}/src; rm -f *.o *.so)
rm -f %{buildroot}%{rlibdir}/R.css
# remove buildroot from installed files
find %{buildroot}%{rlibdir} -type f -exec sed -i "s@%{buildroot}@@g" {} \;

%files
%{rlibdir}/%{packname}
