%global __brp_check_rpaths %{nil}
%global packname  VIFCP
%global packver   1.2
%global rlibdir   /usr/local/lib/R/library

Name:             R-CRAN-%{packname}
Version:          1.2
Release:          3%{?dist}%{?buildtag}
Summary:          Detecting Change-Points via VIFCP Method

License:          LGPL-3
URL:              https://cran.r-project.org/package=%{packname}
Source0:          %{url}&version=%{packver}#/%{packname}_%{packver}.tar.gz


BuildRequires:    R-devel >= 3.1.3
Requires:         R-core >= 3.1.3

%description
Contains a function to support the following paper: Xiaoping Shi,
Xiang-Sheng Wang, Dongwei Wei, Yuehua Wu (2016),
<DOI:10.1007/s00180-015-0587-5>, A sequential multiple change-point
detection procedure via VIF regression, Computational Statistics, 31(2):
671-691.

%prep
%setup -q -c -n %{packname}


%build

%install

mkdir -p %{buildroot}%{rlibdir}
%{_bindir}/R CMD INSTALL -l %{buildroot}%{rlibdir} %{packname}
test -d %{packname}/src && (cd %{packname}/src; rm -f *.o *.so)
rm -f %{buildroot}%{rlibdir}/R.css

%files
%{rlibdir}/%{packname}
