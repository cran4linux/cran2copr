%global packname  BondValuation
%global packver   0.1.0
%global rlibdir   /usr/local/lib/R/library

Name:             R-CRAN-%{packname}
Version:          0.1.0
Release:          1%{?dist}
Summary:          Fixed Coupon Bond Valuation Allowing for Odd Coupon Periods andVarious Day Count Conventions

License:          GPL-3
URL:              https://cran.r-project.org/package=%{packname}
Source0:          %{url}&version=%{packver}#/%{packname}_%{packver}.tar.gz


BuildRequires:    R-devel >= 2.15.1
Requires:         R-core >= 2.15.1
BuildRequires:    R-CRAN-Rcpp 
BuildRequires:    R-CRAN-timeDate 
Requires:         R-CRAN-Rcpp 
Requires:         R-CRAN-timeDate 

%description
Analysis of large datasets of fixed coupon bonds, allowing for irregular
first and last coupon periods and various day count conventions. With this
package you can compute the yield to maturity, the modified and MacAulay
durations and the convexity of fixed-rate bonds. It provides the function
AnnivDates, which can be used to evaluate the quality of the data and
return time-invariant properties and temporal structure of a bond.

%prep
%setup -q -c -n %{packname}


%build

%install

mkdir -p %{buildroot}%{rlibdir}
%{_bindir}/R CMD INSTALL -l %{buildroot}%{rlibdir} %{packname}
test -d %{packname}/src && (cd %{packname}/src; rm -f *.o *.so)
rm -f %{buildroot}%{rlibdir}/R.css

%files
%dir %{rlibdir}/%{packname}
%doc %{rlibdir}/%{packname}/html
%{rlibdir}/%{packname}/Meta
%{rlibdir}/%{packname}/help
%{rlibdir}/%{packname}/data
%{rlibdir}/%{packname}/DESCRIPTION
%{rlibdir}/%{packname}/NAMESPACE
%doc %{rlibdir}/%{packname}/NEWS.md
%{rlibdir}/%{packname}/R
%{rlibdir}/%{packname}/INDEX
%{rlibdir}/%{packname}/libs
