%global packname  SODC
%global packver   1.0
%global rlibdir   /usr/local/lib/R/library

Name:             R-CRAN-%{packname}
Version:          1.0
Release:          1%{?dist}
Summary:          Optimal Discriminant Clustering(ODC) and Sparse OptimalDiscriminant Clustering(SODC)

License:          GPL-2
URL:              https://cran.r-project.org/package=%{packname}
Source0:          %{url}&version=%{packver}#/%{packname}_%{packver}.tar.gz


BuildRequires:    R-devel
Requires:         R-core
BuildArch:        noarch
BuildRequires:    R-CRAN-magic 
BuildRequires:    R-CRAN-ppls 
BuildRequires:    R-CRAN-psych 
BuildRequires:    R-MASS 
Requires:         R-CRAN-magic 
Requires:         R-CRAN-ppls 
Requires:         R-CRAN-psych 
Requires:         R-MASS 

%description
To implement two clustering methods, ODC and SODC, for clustering datasets
using optimal scoring, can also be used as an dimension reduction tool.

%prep
%setup -q -c -n %{packname}


%build

%install

mkdir -p %{buildroot}%{rlibdir}
%{_bindir}/R CMD INSTALL -l %{buildroot}%{rlibdir} %{packname}

test -d %{packname}/src && (cd %{packname}/src; rm -f *.o *.so)
rm -f %{buildroot}%{rlibdir}/R.css

%files
%dir %{rlibdir}/%{packname}
%doc %{rlibdir}/%{packname}/html
%{rlibdir}/%{packname}/Meta
%{rlibdir}/%{packname}/help
%{rlibdir}/%{packname}/DESCRIPTION
%{rlibdir}/%{packname}/NAMESPACE
%{rlibdir}/%{packname}/R
%{rlibdir}/%{packname}/INDEX
