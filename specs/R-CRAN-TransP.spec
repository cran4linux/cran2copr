%global __brp_check_rpaths %{nil}
%global packname  TransP
%global packver   0.1
%global rlibdir   /usr/local/lib/R/library

Name:             R-CRAN-%{packname}
Version:          0.1
Release:          3%{?dist}%{?buildtag}
Summary:          Implementation of Transportation Problem Algorithms

License:          GPL (>= 2)
URL:              https://cran.r-project.org/package=%{packname}
Source0:          %{url}&version=%{packver}#/%{packname}_%{packver}.tar.gz


BuildRequires:    R-devel
Requires:         R-core
BuildArch:        noarch

%description
Implementation of two transportation problem algorithms. 1. North West
Corner Method 2. Minimum Cost Method or Least cost method. For more
technical details about the algorithms please refer below URLs.
<http://www.universalteacherpublications.com/univ/ebooks/or/Ch5/nw.htm>.
<http://personal.maths.surrey.ac.uk/st/J.F/chapter7.pdf>.

%prep
%setup -q -c -n %{packname}


%build

%install

mkdir -p %{buildroot}%{rlibdir}
%{_bindir}/R CMD INSTALL -l %{buildroot}%{rlibdir} %{packname}

test -d %{packname}/src && (cd %{packname}/src; rm -f *.o *.so)
rm -f %{buildroot}%{rlibdir}/R.css

%files
%{rlibdir}/%{packname}
