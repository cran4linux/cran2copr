%global packname  hypothesestest
%global packver   1.0
%global rlibdir   /usr/local/lib/R/library

Name:             R-CRAN-%{packname}
Version:          1.0
Release:          1%{?dist}
Summary:          Confidence Intervals and Tests of Statistical Hypotheses

License:          GPL (>= 2)
URL:              https://cran.r-project.org/package=%{packname}
Source0:          %{url}&version=%{packver}#/%{packname}_%{packver}.tar.gz


BuildRequires:    R-devel
Requires:         R-core
BuildArch:        noarch

%description
Compute the confidence interval of the population mean with one sample or
of the difference of population means of two samples from normal
distributions or t-distributions.Compute the confidence interval of
population variance with one sample or of the difference of population
variances of two samples by chi-square tests.Test for population mean or
the differences of two normal samples under normality with the given null
hypothesis H0,which depends on the user,so that he can know if he can
reject H0 or not at the significance level alpha.Do the chi-square tests
with one or two samples which have multinomial distributions by using an
approximate chi-square distribution when n is large enough.

%prep
%setup -q -c -n %{packname}


%build

%install

mkdir -p %{buildroot}%{rlibdir}
%{_bindir}/R CMD INSTALL -l %{buildroot}%{rlibdir} %{packname}
test -d %{packname}/src && (cd %{packname}/src; rm -f *.o *.so)
rm -f %{buildroot}%{rlibdir}/R.css

%files
%dir %{rlibdir}/%{packname}
%doc %{rlibdir}/%{packname}/html
%{rlibdir}/%{packname}/Meta
%{rlibdir}/%{packname}/help
%{rlibdir}/%{packname}/DESCRIPTION
%{rlibdir}/%{packname}/NAMESPACE
%{rlibdir}/%{packname}/R
%{rlibdir}/%{packname}/INDEX
