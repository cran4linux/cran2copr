%global packname  plmm
%global packver   0.1-1
%global rlibdir   /usr/local/lib/R/library

Name:             R-CRAN-%{packname}
Version:          0.1.1
Release:          3%{?dist}%{?buildtag}
Summary:          Partially Linear Mixed Effects Model

License:          GPL (>= 2)
URL:              https://cran.r-project.org/package=%{packname}
Source0:          %{url}&version=%{packver}#/%{packname}_%{packver}.tar.gz


BuildRequires:    R-devel
Requires:         R-core
BuildArch:        noarch
BuildRequires:    R-CRAN-sm 
BuildRequires:    R-CRAN-Formula 
BuildRequires:    R-nlme 
Requires:         R-CRAN-sm 
Requires:         R-CRAN-Formula 
Requires:         R-nlme 

%description
This package fits the partially linear mixed effects model (semiparametric
random intercept model) using kernel regression, without distributional
assumptions for the random terms. Estimation procedure is an iterative
generalized least squares type. A nonparametric heteroskedastic variance
function is allowed for the regression error. Bootstrap resampling is
provided for inference. The package implements bandwidth selection by an
alternative cross validation for correlated data.

%prep
%setup -q -c -n %{packname}


%build

%install

mkdir -p %{buildroot}%{rlibdir}
%{_bindir}/R CMD INSTALL -l %{buildroot}%{rlibdir} %{packname}

test -d %{packname}/src && (cd %{packname}/src; rm -f *.o *.so)
rm -f %{buildroot}%{rlibdir}/R.css

%files
%dir %{rlibdir}/%{packname}
%doc %{rlibdir}/%{packname}/html
%{rlibdir}/%{packname}/Meta
%{rlibdir}/%{packname}/help
%{rlibdir}/%{packname}/data
%{rlibdir}/%{packname}/DESCRIPTION
%{rlibdir}/%{packname}/NAMESPACE
%{rlibdir}/%{packname}/R
%{rlibdir}/%{packname}/INDEX
